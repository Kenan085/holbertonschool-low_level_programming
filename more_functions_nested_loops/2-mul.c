#include "main.h"

/**
 * mul - Multiplication calculator
 * @a: our first integer to multiply
 * @b: our second integer to multiply
 * Return: system returns the multiplication
 * between a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
